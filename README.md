##### Github Repo Link : [text](https://github.com/MohamadOmarFaruk/DevStack-Frontend)
### Project Name : DevStack
### Description:This Project Is about to figuring out  which stack is the user is using just by adding his known Technologies into
Stack
### Used Technologies : Html,Css,Js,Typescript,React,Tailwind
### Features (Top 3):
          *** First you can add item in the Stacks just by clicking add button
          *** Second You can remove the added items just by clicking Cross Button
          *** You can Click Remove All to remove all the item from the stack

## Q&A:
    Q1 Ans : JSX Stands for Javascript xml its a Syntax extension of react which help us to write html and javascript dynamic expression together it is used in React to provide the user The service of wring html and Javascript Dynamic Expression together

    Q2 ans : The difference betwen props and state is props used in sending data from parent to child means it can send data though one component to another component through one way data binding
    and State is a React feature  That remembers value or store value in a variable and can  dynamically change ui according to change of data

    Q3 ans : The Usestate is used for changing ui dynamacally by the change of state variable data which will be changed by updater function because Usestae gives 2 things one variable that store data and updater function to change the data dynamically
    and i use this hook in my project specically in this Explore.tsx file


    Q4 ans : UseEeffect hook is used to render side effects in the page and of course UseEffect could be used in loading json data but in this project i just use async await method i dont used since it was not manditory

    Q5 ans : In a map react have reconzize different items that he is rendering so thats it uses a key prop which assign an unique id to every element so that react could understand what kind of element he is rendering

    Q6 ans : when in react we want that our ui would apper if this and this condition fulfill we use an condition to show it and
    that condition controls the ui. according to that condition ui changes and rendering is also bind by this condition this whole process is being called conditional rendering in react

    or simply when an rendering process is bind by an condition its called conditional rendering by use of this ui can also change

    i used it in two place in ui in the empty stack text and add button

    Q7 ans : i send data from parent to child i use direct prop and for a child to send data to parent he must have to use calback function a function that would pass down to child by parent but the function itself calls parent and need argument when that kind of function is passed down to an child component of course it will call it than it will need prop and the child component
    will send the data to the function and the parent will receive it because the function exicution will be done in  parent coponent

- Also answer these React questions at the end of your Readme (write the answers in your own words, short and simple):
  1. What is JSX, and why is it used in React?
  2. What is the difference between props and state?
  3. What does the `useState` hook do, and where did you use it in this project?
  4. What does the `useEffect` hook do, and why did you need it to load the JSON data?
  5. Why does every item in a `.map()` list need a unique `key` prop?
  6. What is conditional rendering? Show one place you used it (example: the empty stack message).
  7. How do you pass data from a parent component to a child component, and how does a child send something back to the parent?


- GitHub Repository Link:
- Live Site Link:
